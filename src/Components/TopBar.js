// Importing the React library
import React from 'react';

// Importing the CSS file for the component
import './App.css';

// Defining the TopBar component
function TopBar() {
  return (
    // Rendering a div with the class "top-bar"
    <div className="top-bar">
      <h1>Dashboard</h1>
    </div>
  );
}

// Exporting the TopBar component as the default export
export default TopBar;